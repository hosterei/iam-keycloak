apiVersion: apps/v1
kind: Deployment
metadata:
  name: travelware-keycloak-prod
  namespace: travelware
  labels:
    app: travelware-keycloak-prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: travelware-keycloak-prod
  template:
    metadata:
      labels:
        app: travelware-keycloak-prod
    spec:
      containers:
        - name: travelware-keycloak-prod
          image: quay.io/keycloak/keycloak:17.0.0
          ports:
            - containerPort: 8080
          env:
          - name: DB_ADDR
            value: travelware-keycloak-postgres-prod
          - name: DB_DATABASE
            value: travelware-keycloak-db-prod
          - name: DB_PASSWORD
            valueFrom:
              secretKeyRef:
                name: travelware-keycloak-postgres-prod-postgresql
                key: postgres-password

      